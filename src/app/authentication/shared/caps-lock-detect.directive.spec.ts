import { CapsLockDetectDirective } from './caps-lock-detect.directive';

describe('CapsLockDetectDirective', () => {
  it('should create an instance', () => {
    const directive = new CapsLockDetectDirective();
    expect(directive).toBeTruthy();
  });
});
